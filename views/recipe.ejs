<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800">
    <header class="bg-gray-800 text-white py-6">
        <h1 class="text-3xl font-bold text-center"><%= recipe.title %></h1>
    </header>
    <div class="container mx-auto p-6">
        <img class="mx-auto mb-6 rounded" src="<%= recipe.image %>" alt="<%= recipe.title %>">
        <h2 class="text-2xl font-semibold mb-4">Ingredients</h2>
        <ul class="list-disc list-inside mb-6">
            <% recipe.extendedIngredients.forEach(ingredient => { %>
                <li><%= ingredient.amount %> <%= ingredient.unit %> <%= ingredient.name %></li>
            <% }); %>
        </ul>
        <h2 class="text-2xl font-semibold mb-4">Details</h2>
        <p class="mb-2"><strong>Servings:</strong> <%= recipe.servings %></p>
        <p class="mb-2"><strong>Ready in:</strong> <%= recipe.readyInMinutes %> minutes</p>
        <p class="mb-2"><strong>Source:</strong> <a class="text-blue-500 hover:underline" href="<%= recipe.sourceUrl %>"><%= recipe.sourceName %></a></p>
    </div>
</body>
</html>